{
  "name": "tinker-config-editor",
  "version": "0.1.0",
  "description": "Visual editor for system and application config files",
  "scripts": {
    "dev": "vite build --watch --config ../vite.config.ts",
    "build": "vite build --config ../vite.config.ts"
  },
  "tinker": {
    "name": "Config Editor",
    "icon": "icon.png",
    "main": "dist/index.html",
    "locales": {
      "zh-CN": {
        "name": "配置编辑",
        "description": "系统和应用程序配置文件可视化编辑器"
      }
    }
  }
}
