{
  "name": "tinker-authenticator",
  "version": "0.1.0",
  "description": "2FA authenticator with TOTP generation and QR code scanning",
  "scripts": {
    "dev": "vite build --watch --config ../vite.config.ts",
    "build": "vite build --config ../vite.config.ts"
  },
  "tinker": {
    "name": "Authenticator",
    "category": "productivity",
    "main": "dist/index.html",
    "icon": "icon.png",
    "locales": {
      "zh-CN": {
        "name": "两步验证",
        "description": "两步验证，支持 TOTP 生成和二维码扫描"
      }
    }
  }
}
