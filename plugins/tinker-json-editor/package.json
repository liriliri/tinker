{
  "name": "tinker-json-editor",
  "version": "0.1.0",
  "description": "JSON Editor Plugin for TINKER",
  "scripts": {
    "dev": "vite build --watch --config ../vite.config.ts",
    "build": "vite build --config ../vite.config.ts"
  },
  "tinker": {
    "name": "JSON Editor",
    "description": "JSON editor with text and tree modes",
    "icon": "icon.png",
    "main": "dist/index.html",
    "locales": {
      "zh-CN": {
        "name": "JSON 编辑",
        "description": "JSON 编辑器，支持文本和树形模式"
      }
    },
    "mcp": {
      "tools": {
        "get": {
          "description": "Get the current JSON content in the editor, validation error (if any), editor mode, and line count."
        },
        "set": {
          "description": "Replace the JSON content in the editor. Changes apply immediately. The content may be valid or invalid JSON.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "content": {
                "type": "string",
                "description": "The new JSON content"
              }
            },
            "required": ["content"]
          }
        },
        "format": {
          "description": "Pretty-print the current JSON with 2-space indentation. Fails if the content is not valid JSON."
        },
        "minify": {
          "description": "Minify the current JSON by removing whitespace. Fails if the content is not valid JSON."
        }
      }
    }
  },
  "devDependencies": {
    "@types/jsoneditor": "^9.9.6",
    "jsoneditor": "^10.4.2"
  }
}
